/**
 * Нэг хүний (collaborator) мэдээлэл
 */
export interface Collaborator {
  name: string;
  position: string;
  imageUrl: string; // зурагны link
  // Та хүсвэл socialLinks, email, etc. талбар нэмж болно
}
